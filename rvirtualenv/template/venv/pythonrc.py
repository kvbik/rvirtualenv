
scheme = 'custom'
sitepackages = True
#prompt = '[CUSTOM]' # set your custom prompt prefix (see -p option)
#extra_paths = ['/path/to/inheriting/site-packages/',] # add some if you need access to other venvs

